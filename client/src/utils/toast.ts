import { ToastPosition } from "react-toastify";

const toastConfig = {
  autoClose: 1500,
  closeButton: false,
  pauseOnHover: false,
  pauseOnFocusLoss: false,
  position: "top-center" as ToastPosition,
};

export { toastConfig };
